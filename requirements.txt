# ── Data Ingestion & Streaming ──────────────────────────────────────────────
kafka-python==2.0.2
confluent-kafka==2.3.0          # More robust than kafka-python for prod
fastavro==1.9.4                 # Avro schema serialization for Kafka messages

# ── Big Data Processing ──────────────────────────────────────────────────────
pyspark==3.5.1
pyarrow==15.0.0                 # Fast columnar storage, Spark <-> Pandas bridge
pandas==2.2.1
numpy==1.26.4

# ── Financial & Risk Analytics ───────────────────────────────────────────────
scipy==1.13.0                   # Stats: VaR, covariance, optimization
statsmodels==0.14.2             # GARCH, time series, regression
scikit-learn==1.4.2             # Factor models, clustering
yfinance==0.2.38                # Pull historical stock/ETF data
pandas-datareader==0.10.0       # FRED, Quandl, World Bank feeds
quantlib==1.33                  # Derivatives pricing, yield curves
bt==0.2.9                       # Backtesting framework
empyrical==0.5.5                # Sharpe, Sortino, Max Drawdown, Calmar

# ── Orchestration ────────────────────────────────────────────────────────────
apache-airflow==2.9.1
apache-airflow-providers-amazon==8.19.0   # S3, Glue, Redshift operators
apache-airflow-providers-apache-spark==4.7.1

# ── AWS / Storage ────────────────────────────────────────────────────────────
boto3==1.34.84                  # AWS S3, Glue, Athena
s3fs==2024.3.1                  # Pandas/PyArrow direct S3 read/write
awswrangler==3.7.2              # High-level AWS analytics SDK

# ── Database & SQL ───────────────────────────────────────────────────────────
sqlalchemy==2.0.29
psycopg2-binary==2.9.9          # PostgreSQL
duckdb==0.10.2                  # Local OLAP engine — blazing fast for analytics

# ── Dashboard & Visualization ────────────────────────────────────────────────
plotly==5.21.0
dash==2.16.1
dash-bootstrap-components==1.6.0
dash-ag-grid==31.0.1            # Pro-grade data tables

# ── Config & Utilities ───────────────────────────────────────────────────────
pydantic==2.7.1                 # Config validation, data models
pydantic-settings==2.2.1        # .env / YAML config management
python-dotenv==1.0.1
pyyaml==6.0.1
loguru==0.7.2                   # Way better than standard logging
tenacity==8.3.0                 # Retry logic for API/Kafka/S3 calls
great-expectations==0.18.15     # Data quality validation

# ── Testing ──────────────────────────────────────────────────────────────────
pytest==8.1.1
pytest-cov==5.0.0
moto==5.0.5                     # Mock AWS services locally

dash==2.16.1
dash-bootstrap-components==1.6.0
plotly==5.21.0
pandas==2.2.1
numpy==1.26.4
scipy==1.13.0
yfinance==0.2.38
gunicorn==21.2.0